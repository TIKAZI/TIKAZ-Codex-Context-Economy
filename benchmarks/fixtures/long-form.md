# Context Engineering Note

Context quality depends on relevance, traceability, and recoverability. A smaller prompt is not automatically a better prompt. Evidence must retain stable anchors so a reviewer can return to the source. Budget compliance and protected-fact recall must therefore be reported separately.

# Operational Rule

The evaluation budget is 800 estimated tokens and the review URL is https://example.org/context-review.
